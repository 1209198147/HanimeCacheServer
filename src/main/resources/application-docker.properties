spring.application.name=Hannime

# SQLite Database Configuration
spring.datasource.url=jdbc:sqlite:hannime.db
spring.datasource.driver-class-name=org.sqlite.JDBC
spring.datasource.username=
spring.datasource.password=

spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql
spring.sql.init.data-locations=

# MyBatis-Plus Configuration
#mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl
mybatis-plus.configuration.log-impl=org.apache.ibatis.logging.nologging.NoLoggingImpl
mybatis-plus.global-config.db-config.id-type=input
mybatis-plus.mapper-locations=classpath*:mapper/**/*.xml

# Logging Configuration
logging.config=classpath:logback-spring.xml
logging.level.com.shikou.hannime=DEBUG

# 地区（环境变量 HANNIME_ZONE，默认 HK）
hannime.zone=${HANNIME_ZONE:HK}
# 语言（zhs为简体中文，zht为繁体中文；环境变量 HANNIME_LANG，默认 zhs）
hannime.lang=${HANNIME_LANG:zhs}

# 是否开启缓存任务（开启后任务才会被执行，定期缓存视频；环境变量 CACHE_ENABLE，默认 false）
cache.enable=${CACHE_ENABLE:false}

# 禁用静态资源自动映射，无匹配处理器时抛出NoHandlerFoundException（Spring Boot 4.0+ 默认行为）
spring.web.resources.add-mappings=false
server.servlet.context-path=/

# Tomcat 资源缓存限制（防止超长URL导致OOM）
server.tomcat.resource.cache-ttl=5000
server.tomcat.resource.cache-max-size=10240

# 视频下载路径（环境变量 VIDEO_STORE_PATH，默认 /video-store/）
video.store.path=${VIDEO_STORE_PATH:/video-store/}
# 可选（480P/720P/1080P等 lowest则是最低画质，highest则是最高画质；环境变量 VIDEO_QUALITY，默认 lowest）
video.quality=${VIDEO_QUALITY:lowest}